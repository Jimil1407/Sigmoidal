{
  "name": "api",
  "version": "1.0.0",
  "scripts": {
    "dev": " source venv/bin/activate && uvicorn src.main:app --reload --host 0.0.0.0 --port 8080",
    "start": "source venv/bin/activate && uvicorn src.main:app --host 0.0.0.0 --port 8080",
    "install-deps": "pip install -r requirements.txt",
    "test": "pytest",
    "lint": "flake8 src/",
    "check-types": "mypy src/"
  },
  "dependencies": {
  }
}
